1. defference between var, let, const : 
   var can be re-declare but let and const can't be re declare.
   var is not used in modern JS.
   let can be change the value but const can't be change the value.

2. spread operator expand the element of array or    object.

3. defference between map, filter, forEach:
   map is changed the element and return a new array.
   filter is select element by condition and return a new array.
   forEach is used only for looping and not return a new array.

4. array function is a function which is used on an array.

5. templete literals are used for making a string and variables can be inserted inside a string  ${} .
